<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Quotes Generator</title>
    <link rel="icon" href="/computer.png" type="image/x-icon" />
    <!-- Corrected rel attribute -->
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header>
      
    </header>
    <nav>
      <h1><a href="/">Homepage</a></h1>
      <ul>
        <% // Conditional rendering to display login/signup links based on user authentication status %> 
        <li class="search">
       
            <form>
              <input
              type="text"
              name="query"
              placeholder="Search for quotes..."
              id="searchInput"
            />
            <input type="submit" value="search"  formaction="/api/search" formmethod="get" class="btn"/>
            </form>
         
       
      </li>
        <% if (user) { %>
        <li>Welcome, <%= user.fullname %></li>
        <li><a href="/logout">Log out</a></li>
        <% } else { %>
        <li><a href="/login">Log in</a></li>
        <li><a href="/signup" class="btn">Sign up</a></li>
        <% } %>
      </ul>
    </nav>
  </body>
</html>
